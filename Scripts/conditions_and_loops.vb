' Loop function

    Let x = 1
    Do while x < y
        [Code you want to loop here]
    x = x + 1
    Loop
    
    
' If function
    
    If [condition is true] Then
      [do something]
    Else
      [do other thing]
    End If


'Elseif function (allow multiples "ifs" inside a if function)

    If [condition 1 is true] Then
      [do something]
    ElseIf [condition 2 is true] Then
      [do other thing]
    Else [condition 3 is true] Then
      [do something]
    End If
